@model EntitiesViewModel.TeamLeadViewModel

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TeamLeadViewModel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TeamLeadId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TeamLeadId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    @using (Html.BeginForm("DeleteConfirmed", "TeamLead", FormMethod.Post)) // Replace ControllerName as needed
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.TeamLeadId)

    <input type="submit" value="Delete" class="btn btn-danger" />
 
    @Html.ActionLink("Back to List", "Index")
    }
</div>
